To Compile under Unix:

	make clean
	make
	cd Debug
	export LD_LIBRARY_PATH=${LD_LIBRARY_PATH}:.


To Compile under Windows:
	
	open libexample.dsw in Microsoft Visual Studio


Notes:

	VS C++ Will not compile as a C lib

	GCC Will not compile as a C++ lib

